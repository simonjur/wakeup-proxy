services:
  immich-wake-proxy:
    image: ghcr.io/simonjur/wakeup-proxy:alpha
    # or build locally: build: .
    container_name: immich-wake-proxy
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    # env_file above provides everything; nothing else needed.
