services:
  immich-wake-proxy:
    build: .
    # or: image: ghcr.io/you/immich-wake-proxy:latest
    container_name: immich-wake-proxy
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    # env_file above provides everything; nothing else needed.
